version: "3.8"

services:
  clickup-sync:
    build: .
    container_name: clickup-sync
    restart: unless-stopped
    env_file: .env
    environment:
      - DB_PATH=/app/data/sync.db
    volumes:
      - sync-data:/app/data

volumes:
  sync-data:
